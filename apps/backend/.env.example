# Railway автоматически подставляет DATABASE_URL при добавлении PostgreSQL сервиса
DATABASE_URL="postgresql://user:password@host:5432/dbname"

# Генерировать случайные строки: openssl rand -base64 32
JWT_SECRET="7eRH0AXy5kC0waonuAN0FN5okfmI+g52ffFW/GTOFcI="
JWT_REFRESH_SECRET="другая-случайная-строка"

NODE_ENV="production"
LOG_LEVEL="info"

# PORT Railway подставляет автоматически — не нужно задавать вручную
# PORT=3000
